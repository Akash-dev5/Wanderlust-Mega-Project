version: "3.8"
services:
  mongodb:
    container_name: mongo-service
    image: mongo:4.4
    volumes:
      - ./backend/data:/data
    ports:
      - "27017:27017"

  backend:
    container_name: backend
    image: djangocicd-backend
    build: ./backend
    env_file:
      - ./backend/.env.docker
    ports:
      - "31100:8080"
    depends_on:
      - mongodb

  frontend:
    container_name: frontend
    image: djangocicd-frontend
    build: ./frontend
    env_file:
      - ./frontend/.env.docker
    ports:
      - "8000:5173"

  redis:
    container_name: redis-service
    restart: unless-stopped
    image: redis:7.0.5-alpine 
    expose:
        - 6379
    depends_on:
      - mongodb

volumes:
  data:
